# 行业监控系统 v0.9 — TODO 及开发记录

> 最后更新：2026-05-14 | 当前版本：v0.9  
> 知识产权：娱乐资本论 (https://ylzbl.com/)

---

## 一、已完成事项

### v0.1 — 初始版本（2026-05-13）
- [x] 单文件HTML架构，5个子系统共用同一UI
- [x] 门户首页：5个板块卡片入口
- [x] 子系统仪表盘：统计卡片、信息源管理
- [x] RSS订阅采集 + 网页轮询采集
- [x] 关键字管理 + 告警关键字
- [x] 监控时间线（含搜索、分类过滤）
- [x] AI报告生成（智谱GLM-4-Flash）
- [x] 深色模式切换
- [x] 设置面板：AI配置、导出/导入
- [x] GitHub Pages部署 (https://ylzbl.github.io/monitor/)
- [x] 添加.nojekyll解决Jekyll处理问题

### v0.2 — 修复默认源（2026-05-13）
- [x] 修复默认RSS信息源（替换为真实可用的Feed地址）
- [x] 修复Toast通知不显示问题
- [x] 添加顶部进度条
- [x] 改为顺序采集避免并发超时

### v0.3 — RSS解析备选（2026-05-13）
- [x] 添加rss2json.com作为RSS解析备选方案
- [x] 解决中文RSS通过CORS代理无法正常解析的问题

### v0.4 — 去重与匹配（2026-05-13）
- [x] 修复去重逻辑：分离批次内去重和与已有条目去重
- [x] 改进关键字匹配算法

### v0.5 — Portal数据刷新（2026-05-13）
- [x] 修复从子系统返回首页时数据不刷新
- [x] 重新加载localStorage数据

### v0.6 — 仪表盘可视化 + 自定义AI + 打印（2026-05-13）
- [x] 添加仪表盘可视化：世界地图剪影、饼图、柱状图、折线图
- [x] 构建自定义AI服务商系统：智谱GLM、DeepSeek、Gemini、OpenAI兼容、自定义
- [x] 可自定义AI Prompt（摘要、去重）
- [x] AI连接测试按钮
- [x] 单条AI摘要按钮（时间线条目上）
- [x] 恢复国家电影局、广电总局页面监控
- [x] 打印按钮 + 打印CSS
- [x] 添加"Google News关键字RSS"源类型（gnews）
- [x] 移除设置中重复的深色模式切换

### v0.7 — 关键字相关性 + 修复（2026-05-13）
- [x] 关键字相关性过滤：通用RSS只保留匹配至少一个系统关键字的条目
- [x] 修复AI Key预填问题
- [x] 修复国家电影局URL错误

### v0.8 — 多文件架构 + 合规过滤（2026-05-14）
- [x] 拆分为多文件架构（index.html + monitor.html + common.css + common.js）
- [x] 移除Google News RSS作为默认源
- [x] 新增合规内容过滤（屏蔽大纪元、RFI、DW、BBC、NYT等）
- [x] 页面打开自动运行一次监测
- [x] 新增关键字词云可视化

### v0.9 — 独立子系统页面 + 行业源 + PDF报告（2026-05-14）
- [x] 拆分为5个独立子系统HTML页面（chaowan/guangdian/piaofang/duanju/shangshi）
- [x] 全面替换行业专属信息源（中外玩具网、新腕儿、DataEye、国家电影局等51个源）
- [x] 扩充关键字库（潮玩+谷子/二次元消费，短剧+ROI/出海等）
- [x] 新增出版物风格PDF报告生成（含封面、表格、图表、页眉页脚）
- [x] 首页自动运行监控，新内容时卡片变色+脉冲动画
- [x] 设置面板增加README说明文档
- [x] 优化内容相关性评分
- [x] 合规检查默认开启
- [x] 深色模式等设置项统一收入设置面板
- [x] CSV导出含UTF-8 BOM（Excel兼容）
- [x] JSON导入/导出设置
- [x] 部署到GitHub Pages验证成功

---

## 二、未完成事项

### 高优先级
- [ ] **将README和Changelog自包含到界面中**，点击即可查看说明（当前仅在设置面板中显示纯文本，需要优化为独立可浏览的页面或弹窗）
- [ ] **SVG世界地图**：当前world-map.svg未在子系统中实际渲染使用，需要集成到仪表盘页面
- [ ] **内容相关性深度优化**：当前仅基于标题关键字匹配，需要：
  - 引入正文内容分析（需解决CORS限制获取正文）
  - 语义相似度计算（需AI辅助）
  - 基于历史点击率的协同过滤
- [ ] **定时自动推送**：当前仅有页面打开时自动运行，缺少：
  - 后台Service Worker定时采集
  - 定时推送通知（浏览器需保持打开）
  - 微信/邮件推送集成

### 中优先级
- [ ] **数据持久化升级**：当前localStorage 5MB限制，大量数据时可能溢出，需：
  - IndexedDB存储
  - 数据自动清理策略
- [ ] **信息源健康监控**：采集失败时标记源状态，自动降级/重试
- [ ] **多语言支持**：当前仅中文，考虑英文版
- [ ] **PWA离线支持**：Service Worker + manifest.json
- [ ] **移动端优化**：触摸操作、手势、适配小屏
- [ ] **更多AI服务商预设**：文心一言、通义千问、百川等

### 低优先级
- [ ] **用户账户系统**：云端同步设置和数据
- [ ] **API Key加密存储**：当前明文存储在localStorage
- [ ] **自动化测试**：单元测试、E2E测试
- [ ] **Docker部署方案**
- [ ] **更丰富的报告模板**：月报、专题报告
- [ ] **数据大屏模式**：全屏可视化展示

---

## 三、修改过程与踩坑注意事项

### 1. CORS代理问题 ⚠️
**问题描述**：浏览器直接请求外部网站会被CORS策略阻止。  
**解决方案**：使用corsproxy.io和allorigins.win作为代理。  
**踩坑**：
- 代理不稳定，经常超时（设15s超时）
- rss2json.com作为RSS专用的备选方案更稳定
- 代理链式调用时URL编码需注意双重编码问题
- 中国大陆访问代理可能被墙，需要备用方案

### 2. RSS解析问题 ⚠️
**问题描述**：直接用DOMParser解析RSS XML，中文Feed经常解析失败。  
**解决方案**：优先使用rss2json.com API，失败后回退到本地XML解析。  
**踩坑**：
- rss2json免费版限制10条/请求、10次/分钟
- 部分RSS Feed返回的不是标准XML（含BOM、命名空间等导致解析错误）
- RSS item中的`<link>`标签有时在`href`属性而非文本内容中

### 3. 网页轮询变化检测 ⚠️
**问题描述**：政府网站（广电总局、国家电影局）没有RSS，只能轮询页面变化。  
**解决方案**：DOMParser解析HTML，去除噪音后对比文本快照差异。  
**踩坑**：
- 首次访问无快照，直接返回当前内容（否则永远无数据）
- 页面噪音极多（ICP备案、版权声明、导航等），需PAGE_NOISE正则过滤
- 部分网站用JS动态渲染内容，fetch拿到的是空壳
- 频繁请求可能被封IP（建议间隔>5分钟）

### 4. 合规过滤 ⚠️ 重要
**问题描述**：Google News RSS等源可能返回敏感政治内容。  
**解决方案**：实现BLOCKED_DOMAINS黑名单，默认开启合规检查。  
**踩坑**：
- Google News RSS被用户明确要求移除（不仅过滤，而是根本不用）
- 境外媒体域名变体多（如rfi.fr、rfi.cn、rfi.com等）
- 合规检查需在fetch层面拦截，不能仅在前端显示层面过滤
- 某些RSS item的link域名与实际内容域名不同，需检查redirect后的域名

### 5. AI API调用 ⚠️
**问题描述**：智谱API Key格式特殊，含点号，需注意转义。  
**解决方案**：预填测试Key，支持用户替换。  
**踩坑**：
- 智谱API返回格式与OpenAI略有不同，但兼容
- 免费模型（glm-4-flash）有速率限制，连续请求需间隔
- Prompt过长会截断，建议items控制在30条以内
- API Key明文存在localStorage，安全隐患

### 6. 多文件架构拆分 ⚠️
**问题描述**：从单文件拆分到多文件时，需要处理共享状态。  
**解决方案**：common.js + common.css共享，各子系统页面独立HTML。  
**踩坑**：
- 各子系统页面需独立声明currentSysId
- localStorage数据通过系统ID前缀隔离（mon_{sysId}_xxx）
- 首页(index.html)需要初始化所有5个系统的数据
- 子系统间跳转需重新加载localStorage

### 7. 内容相关性 ⚠️ 持续优化
**问题描述**：通用RSS源返回的内容与行业不相关（如潮玩频道出现AI基建新闻）。  
**解决方案**：关键字匹配过滤 + 相关性评分。  
**踩坑**：
- 短关键字（如"IP"）容易误匹配，需用词边界检测
- 中文分词困难，"短剧"不应匹配"剧"字
- 当前仅基于标题过滤，正文内容无法获取（CORS限制）
- 需要更多行业专属信息源替代通用RSS

### 8. PDF报告生成 ⚠️
**问题描述**：浏览器原生print()输出太原始。  
**解决方案**：生成独立HTML报告页面，在新窗口中打开后调用print()。  
**踩坑**：
- 弹出窗口可能被浏览器拦截，需要fallback下载
- A4打印需要精确控制CSS @page尺寸
- 表格过长会跨页断裂，需page-break-inside:avoid
- 深色模式下打印需强制白色背景

### 9. GitHub Pages部署 ⚠️
**问题描述**：Jekyll处理某些文件会出问题。  
**解决方案**：添加.nojekyll文件。  
**踩坑**：
- GitHub Pages默认使用Jekyll，会处理某些文件
- .nojekyll文件必须在仓库根目录
- 部署后CDN缓存，更新需等1-2分钟
- 自定义域名需CNAME文件

### 10. 数据存储 ⚠️
**问题描述**：localStorage有5MB限制，大量监控数据可能溢出。  
**解决方案**：设置maxItems限制（默认500条）。  
**踩坑**：
- JSON.stringify+parse对大数据量有性能开销
- 隐身模式localStorage可能不可用
- 需要try-catch包裹所有localStorage操作
- 导出CSV需添加UTF-8 BOM（\uFEFF）才能在Excel中正确显示中文

---

## 四、架构概览

```
monitor/
├── index.html          # 门户首页（仪表盘概览）
├── chaowan.html        # 潮玩行业每日监控
├── guangdian.html      # 广电总局数据监控
├── piaofang.html       # 中国电影票房监控
├── duanju.html         # 短剧数据监控
├── shangshi.html       # 娱乐传媒上市公司监控
├── common.js           # 共享核心逻辑（~700行）
├── common.css          # 共享样式（~190行）
└── world-map.svg       # 世界地图剪影SVG
```

### 数据流
```
信息源(RSS/网页) → CORS代理 → 解析(RSS2JSON/DOMParser) 
  → 合规过滤 → 关键字相关性过滤 → 去重 
  → localStorage存储 → UI渲染
  → AI摘要(可选) → 报告生成
```

### 子系统配色
| 子系统 | 主题色 | 色值 | data-sys |
|--------|--------|------|----------|
| 潮玩 | 玫红 | #E91E8C | chaowan |
| 广电 | 蓝色 | #1565C0 | guangdian |
| 票房 | 金色 | #D97706 | piaofang |
| 短剧 | 紫色 | #7C3AED | duanju |
| 上市公司 | 青色 | #0D9488 | shangshi |

---

## 五、关键配置

- **智谱API Key**：已预填测试Key（`89cf52e166744146a966eb905597a5ba.YvQflJOFgM3yX1o8`）
- **CORS代理**：corsproxy.io（主）、allorigins.win（备）
- **RSS解析**：rss2json.com（主）、本地DOMParser（备）
- **GitHub部署**：https://ylzbl.github.io/monitor/
- **合规过滤**：默认开启，屏蔽30+境外敏感域名
- **数据存储**：浏览器localStorage，key前缀 `mon_{sysId}_`
- **版本号规则**：递增0.1，当前v0.9
